import { LinearProgress as LinearProgressMui, LinearProgressProps } from '@mui/material';

export function LinearProgress(props: LinearProgressProps) {
  return (
    <LinearProgressMui {...props} />
  )
}